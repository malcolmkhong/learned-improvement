{
  "name": "continuous-improvement",
  "version": "0.1.0",
  "description": "Self-learning engine for AI coding agents",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "ci": "./dist/bin/ci.js"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.js.map",
    "dist/**/*.d.ts",
    "dist/**/*.d.ts.map"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "continuous-improvement-core": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}